apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-catalog-api
  labels:
    app: catalog-api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: catalog-api
